ALLOW_OPEN_ON_MODIFY=1
ENGINE_ENABLE_IGNORE_GATE=true
RUNNER_HEARTBEAT_SECS =30


########## Paths & Storage ##########
#APP_DB_PATH=runtime/data/app.db
APP_DB_PATH=D:/0 Trading/TGtoMT5/runtime/data/app.db
MT5_ACTIONS_DIR=runtime/actions/inbox   # only used if ROUTER_BACKEND=file
ACKS_DIR=runtime/actions/acks
DEFAULT_LOG_DIR=runtime/logs

# Parser dictionary (absolute or repo‑relative)
PARSER_DICT_YAML=runtime/data/parser_dict.yaml
#ENGINE_ENABLE_RULES=true
ENGINE_ENABLE_RULES=false
ENGINE_SEGMENT_TOP_BLOCK=true
ENGINE_COEXIST_POLICY=true
ENGINE_SLOTS=false
ENGINE_RULES_MODE=semantic   # semantic YAML v2 for OPEN
SEMANTIC_DICT_PATH=runtime/data/parser_semantic.yaml   # semantic rules file
ENGINE_FAILSAFE_ON_UNPARSED=true   # route no-match to UNPARSED/ACK

### to make sure no longer using old yaml approach
DEFAULT_NUM_LEGS=8
#MAX_LEGS=8
MAX_LEGS=20
DEFAULT_LEG_VOLUME=0.01
SIGNAL_REQUIRE_SYMBOL=false
SIGNAL_REQUIRE_PRICE=true

########## Telegram (Telethon) ##########
TG_API_ID=
TG_API_HASH=
TG_PHONE=
# TG_PASSWORD=
# TG_LOGIN_CODE=

TG_SESSION_DIR=runtime/sessions
TG_SESSION_NAME=tg
# TG_SESSION=telethon.session

#TG_SOURCE_CHATS=
#TG_SOURCE_CHATS=
TG_SOURCE_CHATS=

########## Parser & Legs ##########
# --- YAML overrides these parser knobs below; values are ignored at runtime ---
# DEFAULT_NUM_LEGS=5
# DEFAULT_LEG_VOLUME=0.01
TP_DUP_FIRST=true
PARSER_DEBUG=0

HUMAN_LOG_SUMMARY=true   # (true default): show one‑line EXEC summaries in actions_runner.
HUMAN_LOG_PER_LEG=false  # (false default): if true, shows a short line per failed leg.
HUMAN_PRETTY_JSON=true  # (false default): if true, prints a pretty JSON block in addition to your existing JSON line in the router.

########## Router selection ##########
ROUTER_BACKEND=native # file  | native (which means mt5)
ROUTER_MODE=live   # paper | live

########## MT5 (native backend) ##########
MT5_PATH=D:\0 Trading\Platforms\MT5 05 Vantage\terminal64.exe
MT5_LOGIN=
MT5_PASSWORD=^iv
MT5_SERVER=-Demo

MT5_MAGIC=1
MT5_DEVIATION=10
MT5_FILLING=2
SYMBOL_SUFFIX=+
MT5_SUPPRESS_NETTING_WARNING=true

# canonical pips-based knob (recommended)
MT5_FIRST_PRICE_WORSE_PIPS=9.0

# optional overrides for pip size (price units per pip)
PIP_SIZE_OVERRIDES=

########## Unparsed forwarding ##########
UNPARSED_FORWARD_ENABLED=true
UNPARSED_REVIEW_CHAT_ID=-4855564895
UNPARSED_OPS_ACK_CHAT_ID=-4855564895

UNPARSED_LOG_DIR=runtime/logs
UNPARSED_DEDUP_WINDOW_SECONDS=300
UNPARSED_KEEP_DAYS=30

########## Defaults ##########
DEFAULT_SYMBOL=XAUUSD
# SIGNAL_REQUIRE_SYMBOL=false
# SIGNAL_REQUIRE_PRICE=true
SIGNAL_MIN_TEXT_LEN=5
